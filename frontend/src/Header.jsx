import React from "react";
import { Flex, Spacer, Box, Button, IconButton } from "@chakra-ui/react";
import { HamburgerIcon } from "@chakra-ui/icons";

const Header = () => {
  return (
    <header>
      <h1 className="text-3xl text-center text-neutral-600 font-bold">
        Hello World
      </h1>
    </header>
  );
};

export default Header;
